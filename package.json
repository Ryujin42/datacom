{
  "name": "datacom-tooling",
  "private": true,
  "version": "0.0.0",
  "description": "Outillage de depot (hooks Git) uniquement. L'application est un projet Maven/Java, voir pom.xml.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.7"
  }
}
